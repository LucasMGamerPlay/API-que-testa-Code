services:
  codepulse:
    build:
      context: .
    ports:
      - "8765:8000"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=512m,mode=1777,exec
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL